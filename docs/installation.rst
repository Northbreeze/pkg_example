.. highlight:: shell

============
Installation
============


Stable release
--------------

To install pkg_example, run this command in your terminal:

.. code-block:: console

    $ pip install pkg_example

This is the preferred method to install pkg_example, as it will always install the most recent stable release.

If you don't have `pip`_ installed, this `Python installation guide`_ can guide
you through the process.

.. _pip: https://pip.pypa.io
.. _Python installation guide: http://docs.python-guide.org/en/latest/starting/installation/


From sources
------------

The sources for pkg_example can be downloaded from the `Github repo`_.

You can either clone the public repository:

.. code-block:: console

    $ git clone git://github.com/Northbreeze/pkg_example

Or download the `tarball`_:

.. code-block:: console

    $ curl -OJL https://github.com/Northbreeze/pkg_example/tarball/master

Once you have a copy of the source, you can install it with:

.. code-block:: console

    $ python setup.py install


.. _Github repo: https://github.com/Northbreeze/pkg_example
.. _tarball: https://github.com/Northbreeze/pkg_example/tarball/master
